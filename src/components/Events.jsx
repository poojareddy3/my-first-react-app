function Events({ title, date }) {  
   return (
      <div>
      <h2>{ title }</h2>
      <p>Concert</p>
      <p>{ date }</p>
       {/* jsx => JavaScript Expressions- when we fuse HTML & JS */}
      </div>
   )
}

export default Events